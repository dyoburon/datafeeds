# ===========================================
# Email Service Configuration
# ===========================================
# For Gmail, use an App Password (not your regular password):
# 1. Enable 2FA on your Google account
# 2. Go to: https://myaccount.google.com/apppasswords
# 3. Generate a new app password for "Mail"
# ===========================================

EMAIL_USER=your_email@gmail.com
EMAIL_PASSWORD=your_app_password_here
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# Legacy: Single recipient (optional, users are now managed via API)
EMAIL_RECIPIENT=recipient@example.com

# ===========================================
# OpenAI / LLM Service
# ===========================================
OPENAI_API_KEY=your_openai_api_key_here

# ===========================================
# Firebase Admin (Optional - for token verification)
# ===========================================
# If you want the backend to verify Firebase tokens,
# download the service account JSON from Firebase Console:
# Project Settings → Service Accounts → Generate new private key
#
# FIREBASE_SERVICE_ACCOUNT_PATH=./data/firebase-service-account.json
# Or set the JSON directly:
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}

